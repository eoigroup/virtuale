export enum ChatTypes {
  TEXT = "text",
  AUDIO = "audio",
  PHOTO = "photo",
}

export enum ChatEncoding {
  TEXT = "text",
  BASE_64 = "base64",
}

export enum ChatSenderTypes {
  USER = "user",
  ASSISTANT = "assistant",
}
